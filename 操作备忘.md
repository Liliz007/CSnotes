# 操作备忘

> 本文件在项目根目录，不会出现在网站（liliz007.github.io/CSnotes）上。

## 新建笔记

```bash
hugo new content/大二/ads/链表与栈.md
# 生成 content/大二/ads/链表与栈.md，带好 title/date/weight
```

然后在生成的 md 里写内容即可，push 后自动出现在侧边栏对应课程下。

（手动新建也行，只要文件开头有 `title` 和 `weight` 两行 frontmatter。）

## 本地预览

```bash
hugo server
# 打开 http://localhost:1313/CSnotes/
```

## 发布上线

```bash
git add -A
git commit -m "描述这次改动"
git push
# push 后 1–2 分钟自动部署到 https://liliz007.github.io/CSnotes/
```

## 换头像

1. 把图片放进 `static/` 文件夹，命名如 `avatar.png`
2. 修改 `content/_index.md`：`![Liliz](avatar.svg)` → `![Liliz](avatar.png)`
3. `git add -A && git commit -m "更新头像" && git push`
