---
title: 第二章 · 物理层
weight: 2
---

# 1. 基本概念

传输媒体：
* 导引型传输媒体（双绞线、光纤）
* 非导引型传输媒体（微波通信）

物理层协议的主要任务：
* 机械特性：
  * 形状和尺寸
  * 引脚数目和排列
  * 固定和锁定装置
* 电气特性：
  * 指明在接口电缆的各条线上出现的电压的范围
* 功能特性：指明某条线上出现某电平的电压表示何种含义
* 过程特性：规定在信号线上传输比特流的一组操作过程，包括各信号间的时序关系

# 2. 传输媒体

（了解即可）

计算机网络设备之间的物理通路，不属于计网体系结构。

## 导引型

多模光纤：光通过不断的全反射传输
* 由于色散，会导致信号失真
单模光纤：光一直向前传播而不发生全反射
* 直径接近于光的波长，没有色散导致的衰减

## 非导引型

![](../image/2026-09-19-10-19-40.png)

![](../image/2026-09-19-10-20-06.png)

* 无线电波
![](../image/2026-09-19-10-20-36.png)

* 微波
![](../image/2026-09-19-10-20-48.png)
  * 微波是直线传播，而地球是曲面，因此远距离通信需要建立中继站（可以是地面的或卫星）

* 红外
  * 直线传输，中间不能有障碍物（家用遥控器）

# 3. 传输方式

* 串行和并行
* 同步和异步
* 单工、半双工、全双工

## 3.1 串行和并行

![](../image/2026-09-19-10-28-29.png)
![](../image/2026-09-19-10-28-34.png)
* 注意远距离传输是串行

## 3.2 同步和异步

**同步传输：**

![](../image/2026-09-19-10-30-51.png)

字节之间没有间隔，是挨在一起的

如果双方时钟频率存在微小差别，这种累计误差会导致后来接收方采样直接采到相邻bit上

时钟同步方法：
* 外同步：在收发双方之间增加一条时钟信号线
* 内同步：发送端将时钟信号编码到发送数据中一起发送（例如曼彻斯特编码）

**异步传输：**

![](../image/2026-09-19-10-34-28.png)
异步指的是字节之间的时间间隔不固定
但字节中的每个bit仍要同步

## 3.3 单工、半双工和全双工

![](../image/2026-09-19-10-35-13.png)

# 4. 编码与调制

把运送消息的实体(text等)转为比特信号

![](../image/2026-09-19-10-40-49.png)

* 编码：在不改变信号性质的前提下，对波形进行变换
* 调制：把数字基带信号搬到较高频段，产生模拟信号

码元：在使用时间域的波形表示信号时，**代表不同离散数值的基本波形。**

![](../image/2026-09-19-10-45-40.png)

## 常用编码

不归零编码(NRZ,not return to zero)：存在如何分辨一个长时间电平是几个码元的问题，即时钟频率同步问题

归零编码：每个码元传输结束后都要归零（归到零电平），因此接收方在归零后采样即可。
  * 缺点：浪费资源给零电平了

曼彻斯特编码：
![](../image/2026-09-19-10-52-25.png)

* 码元中间时刻的电平跳变既表示时钟信号，也表示数据
* 正跳变表示 1 还是 0，负跳变表示 0 还是 1，可以自行定义
* 问题：带宽开销增大了一倍（两个码元（也就是两个电平）表示一比特数据，因此比特率只有波特率的一半）
  * 波特率：每秒码元数

差分曼彻斯特编码：
![](../image/2026-09-19-11-32-35.png)
* 码元中间时刻的电平跳变仅表示时钟信号，而不表示数据
* 数据的表示在于每一个码元开始处是否有电平跳变：无跳变表示 1，有跳变表示 0
* 优点：
  * 在传输大量连续 1 或连续 0 的情况下，差分曼彻斯特编码信号比曼彻斯特编码信号的变化少
  * 在噪声干扰环境下，检测有无跳变比检测跳变方向更不容易出错，因此差分曼彻斯特编码信号比曼彻斯特编码信号更易于检测
  * 在传输介质接线错误导致高低电平翻转的情况下，差分曼彻斯特编码仍然有效

**4B/5B编码？**

## 基本调制方法

基本调制方法包括
* 调幅AM
* 调频FM
* 调相PM

![](../image/2026-09-19-12-47-00.png)

使用基本调制方法，一个码元只能包含一个bit，因此我们引入混合调制方法：

正交振幅调制QAM（振幅与相位联合调制）

QAM16：16种码元
![](../image/2026-09-19-12-48-49.png)
* 从而一个码元可以表示4bit
* 如何安排各码元对应的bit信息？--使用格雷码

![](../image/2026-09-19-12-51-11.png)
* 任意两个相邻码元只有一位不同。这样可以减少解调错误时错误的位数

# 5. 信道的极限容量

![](../image/2026-09-19-12-53-33.png)
* 码间串扰：码元之间失去清晰界限

**奈奎斯特定理(Nyquist)：**在假定的理想条件下，为避免码间串扰，码元传输速率是有上限的
* 理想低通信道的最高码元传输速率 = 2W Baud = 2W 码元 / 秒
* 理想带通信道的最高码元传输速率 = W Baud
* W：信道的频率带宽（单位为 Hz）
* Baud：波特，即码元 / 秒

>码元传输速率又称为波特率、调制速率等。

实际的信道所能传输的最高码元传输速率，要明显低于奈奎斯特定理给出的上限值。因为奈是理想情况

**香农公式：**带宽受限且有高斯白噪声干扰的信道的极限信息传输速率

![](../image/2026-09-19-13-02-24.png)

在信道的频率带宽W一定的情况下，根据奈奎斯特定理和香农公式，要想提高信息的传输速率，就必须采用多元制（更复杂的调制技术），并努力提高信道中的信噪比。

![](../image/2026-09-19-13-07-25.png)

只需关心某种调制技术能调制出多少个不同的码元即可。

计算时别忘了：不同的码元数量为X，则一个码元能携带的bit数量为$log_2X$